## 计划

### 目标
将备药页「药品清单」药品维度表格中，红框标注的厂商、规格两列字号统一调整为 11px，药品名称和所需数量列保持现有字号不变。

### 修改范围
- `src/routes/m.prep.tsx`：药品维度（drug view）每一行渲染中，厂商列与规格列的 className 改为 `text-caption`（已在 `src/styles.css` 中定义为 11px/18px）。

### 验证
- 保存后在当前预览页（`/m/prep`）确认：
  - 厂商列（不限 / 齐鲁动保）字号为 11px
  - 规格列（1g / 支、100ml / 瓶 等）字号为 11px
  - 左侧药品名称、右侧数量列字号不变
