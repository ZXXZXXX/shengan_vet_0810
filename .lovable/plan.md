调整 `src/components/m/tag-picker.tsx` 中"常用标签"区的视觉与交互，与上方已选区严格分工：

1. 已选标签保留在组件顶部（绿色 chip + ×），点 × 移除。
2. 下方"常用标签"区，对已经在 `selected` 中的标签：
   - 不再渲染为绿色激活态。
   - 改为浅灰底 + 浅灰描边 + 灰色文字的"置灰"样式。
   - 设置 `disabled`，点击无响应（移除统一在上方操作）。
3. 未选中的常用标签保持当前白底可点击样式，点击后即时加入"已选"区。
4. 抽屉内列表保留现有勾选/取消逻辑不变。

仅改 `tag-picker.tsx`，不动调用方。